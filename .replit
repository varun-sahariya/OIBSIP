[deployment]
run = ["gunicorn", "--bind", "0.0.0.0:" + str(os.environ.get("PORT", 5000)), "app:app"]

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "gunicorn --bind 0.0.0.0:$PORT --reuse-port --reload app:app"
waitForPort = 5000
